{
  "name": "arda",
  "version": "0.12.0",
  "description": "meta-flux",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "$(npm bin)/coffee -o lib -c src/*",
    "build": "$(npm bin)/coffee -o lib -c src/*",
    "test": "$(npm bin)/mocha --require espower-coffee/guess test/*.coffee -t 100"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "bluebird": "^2.9.6",
    "cheerio": "^0.18.0",
    "coffee-script": "^1.9.0",
    "coffeeify": "^1.0.0",
    "espower-coffee": "^0.10.0",
    "jsdom": "^4.0.2",
    "mocha": "^2.1.0",
    "power-assert": "^0.10.1",
    "react": "0.13.0",
    "sinon": "^1.12.2",
    "source-map-support": "^0.2.9"
  }
}
